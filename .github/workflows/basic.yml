on: [push]

jobs:
  build:
    runs-on: ubuntu-20.04
    steps:
      # This step checks out a copy of your repository.
      - name: Checkout
        uses: actions/checkout@v2
      - name: Test BASH
        uses: ./.github/actions/test
        id: bash
        with:
          path-to-shell: test/bash
